# 「随心所记」App 专业评审报告

> 评审对象：`index.html`（单文件 PWA，约 1960 行）、`sw.js`、`manifest.webmanifest`
> 评审视角：**程序员（工程/代码质量/健壮性）** + **产品经理（交互流程/体验/信息架构）**
> 评审日期：2026-08-16
> 线上地址：https://1786632695zrj.github.io/suixin/
> 代码仓库：https://github.com/1786632695zrj/suixin

---

## 0. 总体结论（先说结论）

| 维度 | 评分（5 分制） | 一句话结论 |
|------|------|------|
| 视觉与风格一致性 | 4.5 | 极简黑白 + 青色点缀执行得很到位，统一、克制 |
| 核心交互路径 | 4.5 | 主流程（记录→生成→总结→待办）通顺，总结页「今日/周报」双 tab 已修复历史数据断裂 |
| 代码工程质量 | 3.0 | 单文件可用，但存在**存储爆仓隐患**、DOM 膨胀、远端调用无超时等可量产问题 |
| 产品完整度 | 3.0 | 满足 MVP，但缺编辑、归档、跨设备同步、历史可发现性 |
| 可维护性 | 2.5 | 1960 行全在一个 IIFE，无模块、无构建、无测试，迭代成本高 |

**一句话总结**：这是一个**完成度很高的个人 MVP**，视觉与核心理念优秀；「本周/周报」视图不一致问题已在本轮修复（统一为「今日 / 周报」双 tab、周报单框列表、一键生成）。**唯一仍会真实影响使用**的隐患是 ① 图片以 base64 存 `localStorage` 终将撑爆导致数据写不进——已列为 P0 待下一轮处理。其余为体验增强项。

---

## 1. 代码 / 工程视角审查（Programmer Review）

### 1.1 架构与现状

- **单文件架构**：HTML + CSS + JS 全部内联在 `index.html`（276KB，含一张 164KB 兔子 base64）。无打包、无框架、无依赖。
- **存储**：`localStorage` 单键 `diary.v1`，存 `records / todos / notes / summary / weekly / chats / config / meta`。
- **AI**：本地规则引擎（`localSummarize` / `localChatReply`）+ 可选 OpenAI 兼容远端（`chatWithAI` / `remoteSummarize`）。
- **PWA**：`manifest.webmanifest`（图标已 data-URI 内嵌）+ `sw.js`（导航网络优先、静态缓存优先）。

> 评价：对"个人单机 PWA"而言，单文件是**合理取舍**（易分发、易部署）。但当数据量上来后，单键 `localStorage` 会成为天花板。

### 1.2 🔴 严重级（必须修）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| E1 | **图片以 base64 存进 `localStorage`，且无任何配额管理** | `compressImage`→`imageStore`→`save()` | `localStorage` 上限约 **5MB**。一张压缩图约 50–150KB，几十张后即触发 `QuotaExceededError`。当前 `save()` 仅 `toast("保存失败：存储空间不足")`——用户数据会**写不进去且不知为何**。这是会导致 App 不可用的头号隐患。 |
| E2 | **「本周」视图逻辑不一致，违背需求** | `renderWeek` L997–1034 | 有 weekly seed 的周 → 单个卡片（符合"一周一个框"）；**无 seed 的周（包含用户自己每日生成的总结）→ 按"周一/周二…"逐天拆 sub-header**（L1025–1032）。这直接违反你上一轮明确要求"本周就一个框，不分周一到周日"。 |

### 1.3 🟡 中级（建议修）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| E3 | **base64 图片塞进 `onclick` 属性** | `renderRecord` L911、`renderImgPreview` L1860 | 每张图把整段 base64 **同时**放进 `src` 和 `onclick="openLightbox('…')"`，DOM 体积翻倍；大量图片时渲染卡顿。应改用 `data-idx` + 事件委托。 |
| E4 | **远端 AI 调用无超时 / 无 AbortController** | `chatWithAI` L1318、`remoteSummarize` L1269 | 若 DeepSeek 卡顿，对话框会一直显示「…」且发送按钮禁用，用户只能关掉重进。应加 `AbortController` + 超时兜底。 |
| E5 | **导入合并逻辑会覆盖/重复** | `btn-import` L1794–1810 | `summary` 用 `=` 覆盖（丢失本地）；`records/todos` 用 `concat`（同 id 会重复）。应做按 key 去重合并。 |
| E6 | **`apple-touch-icon` 引用 SVG** | `index.html` L12 | iOS 对 `<link rel="apple-touch-icon">` 的 SVG 支持很差（多数版本回退失败），导致"添加到主屏幕"图标异常。应改用 PNG data-URI（manifest 里已是 PNG，这里不一致）。 |
| E7 | **日视图 summary 条目缺 `data-date`** | `renderSummary` L951–966 | 靠 `state.curDate` 兜底。一旦未来在日视图展示非当前日期的总结，编辑会写错日期。建议显式带 `data-date`。 |
| E8 | **删除无二次确认** | 记录删除 L921、随手记删除 L1127 | 日记内容误删不可恢复。建议关键删除加确认（或加"最近删除"暂存）。 |
| E9 | **`render()` 全量重渲** | `render` L1198 | 每次操作都重建所有视图 DOM（即使只改了待办）。当前规模无感，但图片多时会卡。可改为按需渲染当前视图。 |

### 1.4 🟢 轻级（可选优化）

- **E10** `state.curDate` 变更时调用 `save()` 是空操作（curDate 不在 `state.data` 内），属无效写盘，无害但可清理。
- **E11** `localChatReply` 用逗号运算符构造"本地/远端"分支（L1544），可读性差，建议拆 if/else。
- **E12** 无 `Content-Security-Policy` / SRI。个人单机可接受，但作为 PWA 良好实践可加。
- **E13** 无自动化测试。建议至少对 `extractTodos`、`localSummarize`、`weekRange` 加单测（当前仅靠人工验证）。
- **E14** `seed-data` 把 13KB 历史周报内联进 HTML，每次加载都下载。可改为"首次运行时按需 fetch"，减小首屏。

### 1.5 安全性说明

- **API Key 明文存 `localStorage`**：单用户、纯前端 PWA 场景下可接受（无后端即无泄露面），但应**在设置页明确提示"Key 仅存本机浏览器，换设备/清缓存会丢失"**，并建议用户用**额度受限的子账号 Key**。
- 用户输入均经 `escapeHtml` 转义后渲染，无明显 XSS 面。✅

---

## 2. 产品 / 交互视角审查（Product Manager Review）

### 2.1 核心交互路径走查

```
首页（日期 + 云朵兔 + 今日统计）
  └─[记录我的今天]→ 记录页（写文本 + 标签 + 图片 + 自动提取待办）
                    └─[生成今日总结+复盘]→ 总结页（今日/本周）
  └─ Tab：待办（待办/已完成/随手记）
  └─ Tab：总结（今日要点 + 5维复盘；本周单框）
  └─ Tab：设置（AI Key / 导出导入 / 清空）
  └─ 首页 AI 伙伴气泡 → 对话（本地规则 / DeepSeek）
```

**路径合理性**：主链路清晰、符合"随手记→AI 总结→待办"的初衷。Tab 顺序（首页→待办→记录→总结→设置）是你此前指定的，予以保留。

### 2.2 🔴 产品级阻断问题

| # | 问题 | 用户感知 | 建议 |
|---|------|------|------|
| P1 | **「本周」在你自己生成的周里仍按天拆** | 你点"本周"看到的如果是最近这周（无 seed），会是一堆"周一/周二…"小标题，**和你说的"一个框"不一样**，体验割裂。 | 统一为单框：把本周每日生成的 `summary` 聚合成**一个卡片**（不显示周几 sub-header），或干脆显示"本周暂无周报" + 引导去记录页。 |
| P2 | **历史周报不可发现** | 那 4 周工作周报（7/13–8/9）被塞进"本周"视图，但**只有手动用日历切到对应那周一、再点本周**才能看到。新用户根本不知道有这数据。 | 增加「周报存档」：在总结页本周视图下方列出"已有周报：7/13–7/19、7/20–7/26…"可点击直达；或首页增加"历史"入口。 |

### 2.3 🟡 体验增强项

| # | 问题 | 建议 |
|---|------|------|
| P3 | **总结页空态只有提示，无按钮** | 空状态直接放一个「生成今日总结 + 复盘」按钮（复用 `onGenerate`），减少跳页摩擦。 |
| P4 | **记录只能删不能改** | 写错字只能删了重发。建议记录条目长按/点击进入编辑（复用 modal 模式）。 |
| P5 | **跨设备不同步** | 数据只在当前浏览器 `localStorage`，手机和电脑互不打通，换手机/清缓存即丢失。真实日记场景这是硬伤。可选方案：① 导出/导入 JSON 手动迁移；②（进阶）接一个轻量后端/云存储做同步。 |
| P6 | **缺归档/月份视图** | 日记价值在于回看。建议增加"按月"时间线，点月份看该月所有记录与总结。 |
| P7 | **缺每日提醒** | 复盘类 App 靠"持续记录"产生价值。可加应用内提醒（PWA 推送需后端，退而求其次可做"打开时若距上次记录>1天则首页提示"）。 |
| P8 | **AI 伙伴不能帮你写记录** | 目前 AI 只是聊天。可加"把对话里的重要内容一键存为今日记录/待办"，形成闭环。 |
| P9 | **导出格式单一** | 仅 JSON。建议增加 Markdown / PDF 导出，便于分享周报或长期留存。 |
| P10 | **连续记录 streak 只算 records** | 若某天只写了"随手记"或"待办"没算连续。可按需放宽口径。 |

### 2.4 信息架构小建议

- **首页 AI 气泡位置**：首页卡片内右上角与顶部 header 的日期/日历并存，略挤。可考虑把 AI 入口统一收口到 header 右侧（与日历并列），首页只保留"记录"主行动。
- **"待办"页三块**（待办/已完成/随手记）层级清晰，但"随手记"与"记录"页功能有重叠——建议明确区分：**记录=带日期的当日流水**，**随手记=无日期的碎片便签**，并在文案上点明，避免用户困惑"这俩有啥区别"。

---

## 3. 优先级优化路线图

### P0（本周必修，影响可用性）
1. **E1 图片存储改造**：图片迁到 `IndexedDB`（Blob/ObjectURL），`localStorage` 只存索引。彻底解决爆仓。
2. **E2 / P1 本周视图统一**：无 seed 的周也渲染为单框（聚合每日 summary），或空态引导；与你需求一致。
3. **E6 apple-touch-icon 换 PNG**：保证 iOS "添加到主屏幕"图标正常。

### P1（1–2 周内，体验质变）
4. **P2 周报存档列表**：让历史周报可发现。
5. **P3 总结页生成 CTA**：空态内直接可生成。
6. **P4 记录可编辑**：修字不必删重发。
7. **E4 AI 调用超时/Abort**：避免卡死。
8. **E3 图片 DOM 优化**：`data-idx` + 事件委托。

### P2（增强项，按需）
9. **P5 跨设备同步方案**（导出导入先行，后端同步后做）。
10. **P6 月份归档视图**、**P9 Markdown/PDF 导出**。
11. **E5 导入合并去重**、**E7 data-date**、**E8 删除确认**、**E9 按需渲染**。
12. **P7 每日提醒**、**P8 AI 写记录闭环**。

---

## 4. 具体落地建议（给开发）

**E1 最小改造示例（图片 IndexedDB 化）：**
```js
// localStorage 只存图片 id，二进制进 IndexedDB
const imgDB = await openDB("diary-img", 1);
async function putImg(id, blob){ await imgDB.put("imgs", blob, id); }
async function getImg(id){ return await imgDB.get("imgs", id); } // 转 objectURL 用
```
记录里 `images:[id,...]`，渲染时 `URL.createObjectURL(await getImg(id))`，离开页面 `revokeObjectURL`。

**E2/P1 本周统一为单框（伪代码）：**
```js
function renderWeek(c){
  // 有 weekly seed → 直接渲染单框（现状 OK）
  // 无 seed → 聚合本周每日 summary 为单框，不再按天拆：
  const items = days.flatMap(d => getSummary(d.k)?.summary || []);
  c.innerHTML = `<div class="card week-box">
     <div class="card-title">本周总结 <span class="subtle">${rangeLabel}</span></div>
     ${items.map(it=>`<div class="week-ag-item">…${it.text}</div>`).join("") || "本周暂无总结，去记录页生成吧"}
   </div>`;
}
```

**E6 一行修复：**
```html
<link rel="apple-touch-icon" href="data:image/png;base64,iVBORw0KGgo…(复用 manifest 里 512 的 PNG)">
```

---

## 5. 评审清单（Checklist）

- [x] 视觉风格统一、极简克制
- [x] 主交互路径通顺
- [x] 用户输入有转义（无 XSS）
- [x] PWA 可安装、可离线
- [ ] 图片存储不会爆仓（E1）— 本轮未做，见 §6 说明
- [x] 本周视图统一单框（E2/P1）— 已重构为「周报」单框列表
- [x] iOS 图标正常（E6）— apple-touch-icon 已改 PNG data-URI
- [x] 历史周报可发现（P2）— 周报页列出全部周卡片
- [x] 总结页生成 CTA（P3）— 今日视图底部「一键生成周报」
- [ ] 记录可编辑（P4）— 未做（本周报板块已支持编辑）
- [x] 删除有二次确认（E8）— 所有删除（记录/总结/复盘/周报板块/整篇周报）均弹 confirm 确认
- [x] 总结/复盘条目可删除（本轮新增）— 每条「✕」直接删除并联动清理空日期
- [x] 周报可删除（本轮新增）— 整篇「🗑」删除 + 单板块「✕」删除
- [x] 今日模块可浏览/记录本周每天（本轮新增）— 顶部周选择条 Mon~Sun 切换，有内容显示圆点
- [x] 每日总结/复盘可手动添加（本轮新增）— 「＋ 添加一条总结/复盘」行内输入
- [ ] 跨设备同步（P5）— 未做
- [ ] 月份归档（P6）— 未做
- [ ] AI 调用超时保护（E4）— 未做

---

## 6. 本轮落地执行记录（2026-08-16）

> 按本评审报告的路线图，本轮重点修复「总结模块」的交互流程（用户明确要求：周报单列、今日可一键生成）。

### 6.1 已完成的修复

| 项 | 对应报告 | 落地方式 |
|----|----------|----------|
| 总结页「本周」→ 拆为「今日 / 周报」两个 tab | E2 / P1 | `renderSummary` 拆分：`renderDay`（今日视图）+ `renderWeekList`（周报列表视图）；tab 文案「本周」改为「周报」 |
| 周报统一为单框、列出所有周 | E2 / P1 / P2 | `renderWeekList` 按 `weekly[周一key]` 倒序渲染每张"X月X日 ~ X月X日 周报"卡片，含 5 大板块；历史周报（含 seed 4 周）全部可发现 |
| 今日视图显示当日内容 | — | `renderDay` 展示当日 `records`（带图片缩略）+ 当日 `summary`（含 5 维复盘） |
| 一键生成周报 | P3 | 今日视图底部「一键生成周报」按钮 → `generateWeekly()` 聚合本周每日 summary + records 成 5 板块 → 写入 `weekly[周一key]` → 自动切到「周报」页并 toast |
| iOS 图标 | E6 | `apple-touch-icon` 与 favicon 均改为 manifest 内 512 PNG 的 data-URI |
| 周报板块可编辑 | — | 每板块「✎」进入 textarea 编辑，保存即写回 `weekly` |
| SW 缓存刷新 | — | `sw.js` 缓存版本 bump 至 `suixin-v7`，保证更新后自动拉新 |

### 6.2 生成逻辑（`generateWeekly`）

聚合规则：
- **本周工作目标回顾** = 本周每天 `summary[work]` + 当天 `records` 文本（带 `月/日` 前缀）
- **下周工作目标与计划** = 当前未完成的 `todos`
- **AI 进展及计划** = 命中 AI 关键词（`ai/workbuddy/codex/codebuddy/deepseek/智投/看板…`）的复盘/记录
- **成功 case 分享** = 复盘维度为 `achievement` 的条目
- **需要协调和帮助** = 复盘维度 `decision` + 命中协调关键词的记录

### 6.3 本轮未做项（与用户确认后延后）

- **E1 图片 IndexedDB 化（P0）**：本轮用户聚焦交互流程改造，未动图片存储。当前仍按 base64 存 `localStorage`，**几十张图后仍可能触发配额上限**。建议下一轮单独处理，避免影响实时数据写入。
- **E4 AI 调用超时 / AbortController**、**P4 记录编辑**、**P5 跨设备同步**、**P6 月份归档**：均按计划排在 P1/P2，本轮未涉及。

### 6.4 自测

- 通过 jsdom 无头 UI 测试（20 项断言全绿）：首开自动载入 seed（4 周 / 12 天 / 14 待办）→ 进入总结页默认「今日」→ 切到 2026-08-09 显示当日内容 → 点「一键生成周报」自动切「周报」并出现 8月3日~8月9日 卡片（5 板块齐全）→ 周报列表含全部 4 周 → 板块可编辑进入 textarea。

### 6.5 第二轮落地执行记录（2026-08-16 续）

> 用户反馈：① 生成的周报不能删除，不合理；② 今日模块应能浏览/记录本周每一天、且每天可删可改；③ 以专业 PM 视角，所有"用户会想删/改"的地方都要补上删除/修改入口，交互必须完整。

| 项 | 对应报告 | 落地方式 |
|----|----------|----------|
| 周报整篇可删除 | 用户反馈 + E8 | 每张周报卡片标题加「🗑」→ `bindWeekDelete` → `confirm` 确认后 `delete weekly[monKey]` 并重渲染 |
| 周报单个板块可删除 | 用户反馈 + E8 | 每个板块「✕」→ `bindWeekDelete` 的 `[data-week-sec]` → 确认后 `splice` 该板块 |
| 今日模块 = 本周每天 | 用户反馈（P1 增强） | `renderDay` 顶部新增**周选择条 Mon~Sun**：点击切换 `state.curDate`；有内容（summary/records）的日期显示圆点；当前选中高亮 |
| 每日记录可删除 | E8 | 今日视图每条「记录」加「✕」→ `bindDayRecDelete` → 确认后从 `records[date]` 移除 |
| 每日总结/复盘可删除 | E8 | 每条总结/复盘加「✕」→ `bindSummaryDelete` → 确认后 `splice`；该日两类皆空则删除 `summary[date]` 节点 |
| 每日总结/复盘可手动添加 | 用户反馈 | 卡片内「＋ 添加一条总结 / 复盘」→ `addSummaryItem` 行内 textarea，保存写回 `summary[date]` |
| 每日总结可重新生成 | P3 | 当日有记录时显示「生成/重新生成今日总结 + 复盘」按钮 → `onGenerateDay`（与 `onGenerate` 同源逻辑，作用于当前选中日） |
| 删除二次确认 | E8 | 全部删除路径统一走 `confirm()`，关键操作（整篇周报）提示"不可恢复" |
| 行内按钮空间 | 视觉 | 总结/复盘 li `padding-right` 30→56px；新增 `.del-btn/.rec-del/.card-del/.week-strip/.add-link/.add-row` 样式 |
| SW 缓存刷新 | — | `sw.js` 缓存版本 bump 至 `suixin-v8` |

**产品视角补充说明（本轮新增交互）**
- 现在「今日」tab 是一个**完整的最小复盘工作台**：顶部周历一览本周哪几天有内容 → 选中某天看记录+总结+复盘 → 可增、可改、可删 → 底部一键把整周汇总成周报。
- 「周报」tab 从"只能看/改"升级为"可看/改/**可删**"，符合用户对自有内容应有完全控制权的预期。
- 所有破坏性操作均有确认，避免误触丢失日记。

### 6.6 第二轮自测

- jsdom 无头 UI 测试（新增删除/切换断言全绿）：周选择条切换更新 `curDate` 并重渲染 → 删除总结条目后该 li 消失、空日期节点被清理 → 删除周报板块后 `sections` 长度 -1 → 删除整篇周报后 `weekly` 对应 key 消失 → 删除当日记录后 `records[date]` 长度 -1。

---

*本报告基于静态代码走查 + 无头 UI 回归。如需把 P0 的 E1（图片 IndexedDB 化）或 P1 的其余项直接改进并部署，告诉我即可。*
